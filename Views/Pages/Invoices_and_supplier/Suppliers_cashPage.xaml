<Page x:Class="Version3OfERP21_8System.Views.Pages.Invoices_and_supplier.Suppliers_cashPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Version3OfERP21_8System.Views.Pages.Invoices_and_supplier"
      mc:Ignorable="d" 
       xmlns:userControls="clr-namespace:Version3OfERP21_8System.Views.UserControls"
 d:DesignHeight="950" d:DesignWidth="1000"
     Title="Suppliers_cash">


    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../Styles/ButtonStyles.xaml"/>
                <ResourceDictionary Source="../../../Styles/TextBoxStyles.xaml"/>
                <ResourceDictionary Source="../../../Styles/TextBlockStyles.xaml"/>
                <ResourceDictionary Source="../../../Styles/TableStyles.xaml"/>
                <ResourceDictionary Source="../../../Styles/BorderStyles.xaml"/>

                <ResourceDictionary Source="../../../Styles/ComboBoxStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <FontFamily x:Key="MainFont">pack://application:,,,/Assets/#Roboto</FontFamily>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            <BooleanToVisibilityConverter x:Key="StringIsNullOrEmptyToVisibilityConverter" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height="2.1*"/>
            <!-- المدخلات -->
            <RowDefinition Height="1.5*"/>
            <!-- الأزرار -->
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
            <!-- الجدول -->
        </Grid.RowDefinitions>
        <userControls:TitleBar/>


        <!-- قسم المدخلات -->
        <Grid Grid.Row="1" Margin="10,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- العمود الأيمن -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Vertical" Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0 10 10 5">

                    <DatePicker x:Name="txtRegionName" Style="{StaticResource datepickerx1}" />
                    <TextBlock Text="   : تاريخ الفاتوره        " 
                Foreground="White" FontSize="14" 
                VerticalAlignment="Center"  Margin="5 0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 5 0 10">
                    <ComboBox x:Name="cb_Supplly_Name"   Style="{StaticResource SmallerComboBox}"  
                           />
                    <TextBlock Text="  : اسم المورد " Foreground="White" FontSize="16" Width="120"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="RepresentivePercentage" Style="{StaticResource textboxx}" />
                    <TextBlock  Text=" : المبلغ" Foreground="White" FontSize="16" Width="120"/>
                </StackPanel>
            </StackPanel>

            <!-- العمود الأيسر -->
            <StackPanel Grid.Column="0" HorizontalAlignment="Right" Orientation="Vertical" Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                    <TextBox x:Name="mainPrice" Style="{StaticResource textboxx}" Height="75" />
                    <TextBlock Text=" : ملاحظات التوريد" Foreground="White" FontSize="16" Width="150" Height="45"/>
                </StackPanel>

            </StackPanel>

        </Grid>

        <!-- الأزرار -->
        <StackPanel  Grid.Row="2" Background="#1B7F" Margin="40 0 0 0" Orientation="Horizontal">
            <Button Content="حذف" Width="60"  Style="{DynamicResource DeleteButtonn}" Margin="10"  />
            <Button Content="إضافة" Width="70"   Style="{DynamicResource AddButtonn}" Margin="20,0"  />

        </StackPanel>

        <!-- قسم الجدول -->
        <DataGrid 
      Grid.Row="3"
      x:Name="DataGridOfSupplyCash"
      AutoGenerateColumns="False"
      CanUserAddRows="False"
      CanUserDeleteRows="False"
      IsReadOnly="False"
      SelectionMode="Single"
      SelectionUnit="FullRow"
      ScrollViewer.VerticalScrollBarVisibility="Auto"
      ScrollViewer.HorizontalScrollBarVisibility="Auto"
      FontSize="14"
      RowHeight="25"
      Background="White"
      HeadersVisibility="Column">

            <DataGrid.Columns>

                <DataGridTextColumn Binding="{Binding Supplly_Notes}" Header="ملاحظات التوريد" Width="*"/>
                <DataGridTextColumn Binding="{Binding Supplly_Price}" Header="المبلغ المورد" Width="*"/>
                <DataGridTextColumn Binding="{Binding SuppliersName}" Header="اسم المورد" Width="1.5*"/>
                <DataGridTextColumn Binding="{Binding SupplyDate}" Header="تاريخ التوريد" Width="*"/>
                <DataGridTextColumn Binding="{Binding Suppliers_cashId}" Header="رقم المسلسل" Width="1*"/>
            </DataGrid.Columns>

        </DataGrid>

        <Button Grid.Row="4" HorizontalAlignment="Left"   CommandParameter="DashBoardDoctor" 
      Content="↩" FontSize="22" Style="{StaticResource triggerrr}"
      Margin="21,10,0,1"  
      Click="BackBtn_Click"/>

    </Grid>

    
</Page>
